using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class setBool : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Movement.overworld = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
